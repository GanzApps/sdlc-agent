# Skills

This folder contains reusable workflow helpers.

Use `skills/<name>/SKILL.md` for:
- shared setup guidance
- reusable checks
- common handoff patterns
- future cross-agent workflow modules

A skill represents how work is performed, not who owns the role.
