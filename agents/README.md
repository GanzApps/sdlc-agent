# Agents

This folder contains agent role definitions.

Use `agents/<role>/AGENT.md` for:
- role identity
- required connectors
- phase ownership
- role-specific rules
- expected outputs and handoff behavior

An agent represents who is acting in the framework.
