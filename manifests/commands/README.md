# Command Contracts

Each command contract defines:
- required inputs
- required upstream artifacts
- readiness checks
- expected outputs
- allowed statuses
- blocked conditions

These files are meant to be copied into installed workspaces under `.ai-sdlc/manifests/commands/`.
