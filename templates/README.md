# Templates

This folder holds reusable framework templates that should be copied into installed workspaces under `.ai-sdlc/templates/`.

Current templates:
- `handoff-packet.md`: canonical handoff packet contract
- `handoffs/*.md`: one worked example per agent role
